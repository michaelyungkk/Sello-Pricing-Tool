
import React from 'react';

// This section is currently unused in the Settings/Configuration page 
// but is reserved for future settings migration (e.g. from UserProfile).
export const PersonalizationSection: React.FC = () => {
    return null;
};
